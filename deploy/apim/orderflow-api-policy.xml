<policies>
    <inbound>
        <base />

        <!-- 1. CORS first: the browser's OPTIONS preflight carries no token and no key. -->
        <cors allow-credentials="false">
            <allowed-origins>
                <origin>{{spa-origin}}</origin>
                <origin>http://localhost:4200</origin>
            </allowed-origins>
            <allowed-methods preflight-result-max-age="300">
                <method>GET</method>
                <method>POST</method>
                <method>OPTIONS</method>
            </allowed-methods>
            <allowed-headers>
                <header>Content-Type</header>
                <header>Authorization</header>
                <header>Idempotency-Key</header>
                <header>Ocp-Apim-Subscription-Key</header>
                <!-- W3C trace context from the browser SDK (Day 2, 2P) -->
                <header>traceparent</header>
                <header>tracestate</header>
                <header>request-id</header>
                <header>request-context</header>
            </allowed-headers>
            <expose-headers>
                <header>Location</header>
                <header>X-Correlation-Id</header>
            </expose-headers>
        </cors>

        <!-- 2. Rate limit per subscription: 30 calls / 60 s -> 429 + Retry-After. -->
        <rate-limit calls="30" renewal-period="60" />

        <!-- 3. JWT: ALWAYS. Signature, issuer, audience, lifetime — and the delegated scope. Roles are
                checked by Order, which knows what each role may do; APIM only knows "is this a
                legitimate caller of this API at all". -->
        <validate-jwt header-name="Authorization" require-scheme="Bearer"
                      failed-validation-httpcode="401"
                      failed-validation-error-message="Missing or invalid access token.">
            <openid-config url="https://login.microsoftonline.com/{{tenant-id}}/v2.0/.well-known/openid-configuration" />
            <audiences>
                <audience>{{api-client-id}}</audience>
                <audience>api://{{api-client-id}}</audience>
            </audiences>
            <issuers>
                <issuer>https://login.microsoftonline.com/{{tenant-id}}/v2.0</issuer>
                <issuer>https://sts.windows.net/{{tenant-id}}/</issuer>
            </issuers>
            <required-claims>
                <claim name="scp" match="any" separator=" ">
                    <value>Orders.ReadWrite</value>
                </claim>
            </required-claims>
        </validate-jwt>

        <!-- 4. Correlation id: reuse the caller's or mint one. The services tag their spans with it. -->
        <set-header name="X-Correlation-Id" exists-action="skip">
            <value>@(context.RequestId.ToString())</value>
        </set-header>

        <!-- 5. The subscription key is APIM's business; the backend never sees it. -->
        <set-header name="Ocp-Apim-Subscription-Key" exists-action="delete" />
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <base />
        <!-- Response transformation: don't advertise the stack. -->
        <set-header name="Server" exists-action="delete" />
        <set-header name="X-Powered-By" exists-action="delete" />
        <set-header name="X-Correlation-Id" exists-action="override">
            <value>@(context.RequestId.ToString())</value>
        </set-header>
    </outbound>

    <on-error>
        <base />
        <set-header name="X-Correlation-Id" exists-action="override">
            <value>@(context.RequestId.ToString())</value>
        </set-header>
    </on-error>
</policies>
